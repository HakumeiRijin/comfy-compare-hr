# Changelog

このプロジェクトの主な変更履歴。Gitでの管理を開始する前の試行錯誤も、
記録として残すためにここにまとめる。

## v0.1.0 - 初回リリース(Git管理開始時点)

### MVP構築(実装仕様書 v4 ベース)
- uvプロジェクトの初期構築
- PySide6による基本ウィンドウ
- Explorerからのドラッグ&ドロップ(複数ファイル対応)
- 最大8枚のタイル表示、枚数に応じた自動レイアウト(1/2/3/4列)
- アスペクト比を維持した画像表示、初期Fit表示
- 全画像同期のズーム・パン
- 画像選択(視覚的ハイライト)
- 壊れた画像・存在しないファイルなどのエラーハンドリング

### 第1弾の改善
- ズーム基準点を「カーソル追従」から「各タイルの枠の中心固定」に変更
  (パン後にズームしても枠中心が不動の軸になるよう修正)
- 選択中ファイル名をタイトルバーに表示
- 選択した画像をタイル表示から除外する機能(ファイル自体は削除しない)
- 削除後の空き枠への再ドロップ対応
- バグ修正: 0枚に戻った際のウィジェット破棄クラッシュ
  (`setCentralWidget`の再利用が原因。`QStackedWidget`で解決)
- バグ修正: 画像削除後に列数が正しく詰まらない問題
  (`QGridLayout`のストレッチ設定が残留していたのが原因)

### 第2弾の改善
- 同一ファイルの重複ドロップを防止(パスを正規化して比較)
- 右クリックでFit⇔100%表示をトグルする機能を追加
  (各種操作後は必ずFitから開始するよう統一)
- 選択中の画像を再クリックすると選択解除されるように変更
- ファイル名・倍率をタイル上部にオーバーレイ表示する機能(表示/非表示切り替え可能)

### ChatGPTによるレビュー対応
- 無関係なドロップ(重複ファイルのみ等)で表示状態がリセットされる不具合を修正
- 未使用だった`cursor_pos`引数を削除
- ホイールイベントで`angleDelta().y() == 0`のケース(タッチパッド等)を無視するよう安全策を追加

### 運用改善
- キーボードショートカットの整理(`Ctrl+0`廃止、`C`キーで全画像削除を追加)
- `run.bat`によるダブルクリック起動に対応

### 操作体系の全面刷新(マウス操作への一本化)
「画像閲覧という用途にはマウス操作への集約が適しており、キー操作は不要」との
判断のもと、以下の通り操作体系を全面的に見直した。

- 選択機能を廃止(タイトルバー表示・Dキー削除の対象特定という2つの役割は
  右クリックメニューとオーバーレイ表示に統合)
- 左クリック: Fit⇔100%表示トグル専用に変更
- 右クリック: コンテキストメニューを表示
  (このタイルを削除/すべて削除/オーバーレイ表示切替/Fit表示にリセット)
- キーボード操作(F/D/V/C)をすべて廃止し、マウスのみで全操作が完結する構成に変更

この過程で発生した細かい不具合(メニュー表示中の左クリック誤発火、
右クリックでのメニュー誤選択など)も合わせて修正済み。
特に「メニューを閉じるためのクリックが、閉じた直後に新しい独立した
クリックイベントとして背後のウィジェットに配送される」というQtの挙動に
起因する不具合は、発生ログを取得して原因を特定し、メニューが閉じた後も
一定時間(100ms)入力を無視する対策で解決した。

## v0.1.1

### メニュー項目の整理
- 右クリックメニューから「Fit表示にリセット」を削除
  (左クリックのFit⇔100%トグルと役割が完全に重複していたため)
- メニュー項目の日本語表記を統一(「このタイルを削除」「すべてのタイルを削除」
  「オーバーレイ表示をオン/オフ」)

### バグ修正: Fit⇔100%トグルが1回目だけ空振りする問題
「次に何をすべきか」を`is_100_percent_mode`という独立したフラグで管理していたが、
ズーム・パン等の他の操作で実際の表示状態が変わった際にフラグとの整合性が崩れ、
「起動直後や各種操作直後の1回目のクリックで見た目が変化しない」不具合があった。
フラグを廃止し、`shared_zoom == 1.0`かどうかで「現在ちょうどFit状態か」を
都度判定する方式に変更して解消した。

### 右クリックメニューの実装をリファクタリング
右クリック処理を`mousePressEvent`内で直接`menu.exec()`を呼ぶ実装から、
Qt標準の`contextMenuEvent`を使う実装に全面的に書き直した。

これは、ChatGPTによるセカンドオピニオンをきっかけに、それまで対症療法的に
積み重ねていたフラグ・タイマー・世代カウンタ等の複雑な保護策
(`suppress_input`、`_context_menu_generation`など)を一掃し、
根本原因(右クリックの処理経路と左クリックの処理経路が構造的に分離されて
いなかったこと)に対処する形にしたもの。結果としてコード量も大幅に削減された。

### バグ修正: メニューを閉じた直後の左クリック誤発火(再発・再修正)
メニュー外をクリックして閉じる操作は、Qtの仕様上「メニューを閉じる」ことと
「その位置への通常のクリック」を兼ねる。そのため`contextMenuEvent`化した後も
問題が再発したが、原因はイベント処理の構造ではなくQtの正当な仕様であることを
発生ログで確認した。メニューが閉じた時刻を記録し、閉じた直後の短い間(0.2秒)に
発生した左クリックを無視する、シンプルな時間ベースの判定で解消した。

### 見送った変更: 同じ位置での右クリック連打時のメニュー再表示
「同じ位置で右クリックするとメニューが閉じてしまう(再表示されない)」挙動の
改善を試みたが、`QApplication.mouseButtons()`や`processEvents()`を用いた
判定はQtの内部的なイベント処理と相性が悪く、条件によっては
再表示ループが上限(20回)まで回ってしまう、メニューが固まって動かなくなる
等、かえって不安定になることが実機検証で判明したため見送った。
この挙動自体はQtの標準的な仕様であり実害が小さいため、現状維持とする。

## v0.2.0

### アプリアイコンの設定
- ウィンドウのタイトルバー・タスクバーに表示されるアイコンを設定できるように対応
- アイコンファイルは `assets/icon.ico` に配置する運用とし、
  `pyproject.toml`と同じ階層に直接置くよりも分かりやすい構成にした
- アイコンファイルが存在しない場合でもクラッシュせず、
  デフォルトアイコンのまま起動を継続する
- 起動用の`run.bat`のショートカットにも同じアイコンを設定することで、
  ショートカット・ウィンドウ・タスクバーの見た目を統一できるようにした

## v0.2.1

### 依存パッケージの軽量化: PySide6-Essentialsへの切り替え
依存関係を`PySide6`から`PySide6-Essentials`に変更した。

このアプリで実際に使用しているモジュール(`QtWidgets`、`QtCore`、`QtGui`)は
すべて`PySide6-Essentials`に含まれており、`QtWebEngine`や`QtMultimedia`等の
未使用の`Addons`機能を除外できるため、コードの変更は一切不要だった。
実機検証の結果、`.venv`フォルダの容量が約682MBから約230MBまで削減され、
体感でも明確な軽量化効果が確認できた。

### レイアウト調整: 3枚表示時のみ3列に変更
枚数→列数の対応表で、3枚のときだけ特別に3列(横一列)にするよう変更した。

これまでは3枚の場合も2列扱いとなり、2行目に3枚目だけが孤立して配置され、
4マス中2マスが空白になっていた。主用途である縦長画像の比較において、
これが他の枚数(5枚→3列、7枚→4列)よりも視覚的な据わりの悪さが際立って
気になっていたため。5枚・7枚のケースは空白の割合が3分の1・4分の1と
小さいのに対し、3枚(旧2列)は空白が2分の1と大きかったことが要因と考察している。

実機検証の結果、縦長画像では閲覧性が明確に向上し、横長画像でも
既存の2列表示と比べて大きな悪化はないことを確認した。
なお、4枚以降を横一列にする案も検討したが、画像の表示幅が
狭くなりすぎるため見送り、3枚のみの変更にとどめた。

## v0.3.0

### メタデータ表示機能の追加(最小版)
右クリックメニューに「メタデータを表示」を追加し、選択すると
画像に埋め込まれたテキストメタデータを別ウィンドウで確認できるようにした。

「ComfyUIで生成した画像のメタデータを、比較ツール内で閲覧したい」という
大目標に向けた最初の一歩として、まずは最小限の実装から着手した。

- 対象はPNGのtEXt/iTXtチャンク(Pillowの`img.text`)のみ。ComfyUIが書き込む
  `prompt`・`workflow`等のキーごとに、生のJSON文字列をそのまま表示する
  (整形・解釈は行わない)
- JPEG等のEXIF、HEIF、MakerNote解析などは対象外とし、追加パッケージが
  必要になるものには踏み込まない方針とした
  (`inspect_metadata.py`での事前調査で線引きを決めた)
- タブ分け(Overview/AI/EXIF/Technical/Rawなど)は行わず、
  見つかったキーを縦に並べて表示するだけの、単一ウィンドウ構成とした
- 実装は`src/image_compare/metadata/`サブパッケージに分離し、
  読み取りロジック(`reader.py`)と表示ロジック(`viewer_window.py`)を分けた
- 複数の画像のメタデータウィンドウを同時に開けるようにし、
  メインウィンドウ・他のメタデータウィンドウの操作を妨げないようにした

タブ分けやタイル配置との連動、スクロール同期などは、実際に使ってみて
必要性を見極めてから追加するかどうかを判断する。